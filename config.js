const CONFIG = {
    // Mode: 'server' (uses local Node server) or 'client' (uses legacy Google Login)
    mode: 'server',

    // API URL
    apiUrl: 'http://localhost:3000/api',

    // UI Settings
    itemsPerPage: 50,
    notificationDuration: 3000
};
